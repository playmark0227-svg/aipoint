/* ===================================================================
   えにわ愛ポイント｜管理画面（ヘッドレスCMS）接続設定
   -------------------------------------------------------------------
   ここを書き換えるだけで、管理画面と接続されます。
   （納品時に制作側で設定します。設定前でもサイトは正常に表示されます）

   1. microCMS の管理画面 → サービス設定 でサービスIDを確認
   2. 管理画面 → API設定 → APIキー で「GET」権限のキーを発行
   3. 下記の 2 か所を書き換えて保存

   ※ APIキーは「GET（参照）のみ」の権限で発行してください。
     書き込み権限のあるキーは絶対にここに書かないでください。
   =================================================================== */
window.AIPOINT_CMS = {
  /* microCMS のサービスID（https://〇〇〇.microcms.io の〇〇〇の部分） */
  serviceDomain: 'YOUR-SERVICE-ID',

  /* microCMS の APIキー（GET＝参照のみの権限で発行） */
  apiKey: 'YOUR-API-KEY',

  /* 開発時にコンソールへ状況を出す場合は true */
  debug: false
};
