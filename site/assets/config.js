/* ===================================================================
   えにわ愛ポイント 公式ポータルサイト｜サイト設定ファイル
   -------------------------------------------------------------------
   ★ サイトの動作設定は、このファイルだけを書き換えれば変更できます。
     HTMLを触る必要はありません。
     未設定の項目があっても、サイトはエラーにならず正常に表示されます。
   =================================================================== */
window.AIPOINT_CONFIG = {

  /* ---------------------------------------------------------------
     1) 管理画面（ヘッドレスCMS：microCMS）
     ---------------------------------------------------------------
     お知らせ・加盟店を管理画面から更新するための設定です。
     ※ APIキーは必ず「GET（参照）のみ」の権限で発行してください。
        書き込み権限のあるキーは絶対に設定しないでください。          */
  cms: {
    serviceDomain: 'YOUR-SERVICE-ID',   // https://〇〇〇.microcms.io の〇〇〇
    apiKey:        'YOUR-API-KEY',      // GET（参照）のみのAPIキー
    newsEndpoint:  'news',              // お知らせのAPI ID
    shopsEndpoint: 'shops'              // 加盟店のAPI ID
  },

  /* ---------------------------------------------------------------
     2) フォーム送信先
     ---------------------------------------------------------------
     お問い合わせフォーム・愛称投票フォームの送信先です。
     Formspree / Google Apps Script など、JSONを受け取れるURLを設定します。
     空欄のままでも画面は動作し、送信時に「メールでのご連絡」を案内します。 */
  forms: {
    contactEndpoint:  '',   // 例: https://formspree.io/f/xxxxxxx
    voteEndpoint:     '',   // 例: https://script.google.com/macros/s/xxxx/exec
    fallbackMail:     'info@eniwa-point.com'   // 未設定時の案内先
  },

  /* ---------------------------------------------------------------
     3) アクセス解析（Google Analytics 4）
     ---------------------------------------------------------------
     測定ID（G-XXXXXXXXXX）を設定すると計測が有効になります。      */
  analytics: {
    ga4Id: ''               // 例: G-XXXXXXXXXX
  },

  /* ---------------------------------------------------------------
     4) 外部リンク
     --------------------------------------------------------------- */
  links: {
    appDownload: 'https://onelink.to/rmdmub',   // アプリDL（Apple/Android両対応）
    facebook:    '',
    line:        '',
    instagram:   ''
  },

  /* ---------------------------------------------------------------
     5) 事務局情報（フォームの案内文などに使用）
     --------------------------------------------------------------- */
  office: {
    name:  '恵庭駅通商店街振興組合',
    admin: '株式会社北央商事',
    tel:   '0123-34-0828',
    mail:  'info@eniwa-point.com'
  },

  /* 開発時にコンソールへ動作状況を出す場合は true */
  debug: false
};

/* 旧設定名との互換（既存の cms.js 用） */
window.AIPOINT_CMS = window.AIPOINT_CONFIG.cms;
